export default class ConfirmMove {
    constructor($scope, ganttService, dataService) {
        this.$scope = $scope;
        this.ganttService = ganttService;
        this.dataService = dataService;  
    }
}

